'use strict';

// MODULES //

var extractAcct = require( './extractAcct.js' );


extractAcct( 27754 );

/*
var rl = require('readline').createInterface({
	input: require('fs').createReadStream('file.in')
});

rl.on('line', function (line) {
  console.log('Line from file:', line);
});
*/
